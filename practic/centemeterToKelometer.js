//writ a function to convert centemeter to kelometer.
function centemeterTokelo(centemeter = 0) {
    return kelometer = parseFloat((centemeter / 100000).toFixed(3));
}
const totalKelometer = centemeterTokelo(50)
console.log(totalKelometer)